package ru.dr.meterreadings.models.domain

/**
 * ========================================
 * МОДЕЛИ ДЛЯ ДАННЫХ ОТ ПРОВАЙДЕРОВ
 * ========================================
 *
 * Эти модели используются для данных которые
 * загружаются с сайтов провайдеров и НЕ хранятся в БД.
 *
 * Данные существуют только в памяти и обновляются
 * при каждом подключении к провайдеру.
 */

/**
 * Информация о лицевом счёте от провайдера
 *
 * Загружается с сайта провайдера, не хранится в БД.
 *
 * ПОКА содержит только адрес, будем расширять
 * по мере изучения реальных данных от КВЦ.
 */
data class AccountInfo(
    /**
     * Адрес недвижимости
     *
     * Пример: "г. Нижний Новгород, ул. Ленина, д. 5, кв. 12"
     */
    val address: String
)

/**
 * Информация о счётчике (прибор учёта)
 *
 * Создадим когда увидим реальные данные от КВЦ
 */
// data class MeterInfo(...)

/**
 * Показание счётчика
 *
 * Создадим когда увидим реальные данные от КВЦ
 */
// data class ReadingInfo(...)
